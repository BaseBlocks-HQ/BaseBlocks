/** Visual indentation in pixels per depth level. */
export const INDENT_WIDTH = 20;

/**
 * Horizontal pixel step per depth level change during self-hover drag.
 * Only used when dragging over your own ghost to un-nest.
 */
export const DRAG_INDENT_STEP = 40;

/** Delay before auto-expanding a collapsed folder on hover during drag (ms). */
export const AUTO_EXPAND_DELAY_MS = 600;
